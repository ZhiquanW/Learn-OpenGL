#pragma once
#include "common_includes.h"
namespace dawn_engine {

class DawnUIManager {
  private:
  protected:
  public:
    DawnUIManager(GLFWwindow *glfwWindow);
    ~DawnUIManager();
};
} // namespace dawn_engine